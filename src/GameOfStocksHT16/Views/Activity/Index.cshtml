@model GameOfStocksHT16.Models.ActivityViewModels.ActivityModel

@{
    ViewData["Title"] = "Aktivitetssida";
}
<section class="section-padding-medium">
    <div class="container">
        <h3>Aktiviteter</h3>
        <hr />
        @foreach (var activity in Model.Activities)
        {
            <img src="@activity.User.PictureUrl" width="75" height="75" class="img-circle" />
            <p class="">
                @activity.User.FullName har
                @if (activity.IsBuying)
                {
                    <span>
                        kÃ¶pt
                    </span>
                }
                else
                {
                    <span>
                        sÃ¥lt
                    </span>
                }
                @activity.Quantity st @activity.Name aktier.
            </p>
            <hr />
        }

<<<<<<< HEAD


<div class="container">
    <div class="col-md-offset-1">
        <div class="row text-center">
            <h2>Aktiviteter</h2>
        </div>
 
        @foreach (var activity in Model.Activities)
        {
            @if (activity.User.Email != User.Identity.Name)
            {
                if (activity.IsBuying && !activity.IsCompleted)
                {
                    <div class="panel panel-default text-center">
                        <div class="panel-heading">
                            <div class="row">
                                <img class="img-circle" src="@activity.User.PictureUrl" height="40" width="40" />
                            </div>
                            <p>@Html.ActionLink(@activity.User.FullName, "VisitProfile", "Users", new { email = @activity.User.Email }) har köpt @activity.Quantity aktier från @Html.ActionLink(@activity.Name, "Stock", "StockList", new { label = @activity.Label })</p>
                            <p><em>@activity.Date</em></p>
                        </div>
                    </div>
                }
                else if (activity.IsSelling && !activity.IsCompleted)
                {
                    <div class="panel panel-default text-center">
                        <div class="panel-heading">
                            <div class="row">
                                <img class="img-circle" src="@activity.User.PictureUrl" height="40" width="40" />
                            </div>
                            <p>@Html.ActionLink(@activity.User.FullName, "VisitProfile", "Users", new { email = @activity.User.Email }) har sålt @activity.Quantity aktier från @Html.ActionLink(@activity.Name, "Stock", "StockList", new { label = @activity.Label })</p>
                            <p><em>@activity.Date</em></p>
                        </div>
                    </div>
                }

            }
        }
    </div>
</div>
=======
    </div>
</section>
>>>>>>> origin/master
